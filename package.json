{
	"name": "molecu-library",
	"version": "1.0.0",
	"description": "Proyecto de una librería con front en Nuxt y Back en Molecular",
	"keywords": [],
	"license": "ISC",
	"author": "abbytec",
	"main": "index.js",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:api": "npm run dev --workspace=api",
		"dev:front": "npm run dev --workspace=frontend",
		"dev": "concurrently \"npm run dev:api\" \"npm run dev:front\"",
		"build:shared": "npm run build --workspace=shared",
		"build:api": "npm run build --workspace=api",
		"build:front": "npm run build --workspace=frontend",
		"build": "npm run build:shared && npm run build:api && npm run build:front",
		"build:prod": "npm run build",
		"start:api": "PORT=8181 npm run start --workspace=api",
		"start:front": "npm run preview --workspace=frontend",
		"start": "concurrently \"PORT=8181 npm run start:api\" \"npm run start:front\"",
		"clean": "npm run clean --workspace=shared && rm -rf apps/*/dist apps/*/node_modules/.cache",
		"install:all": "npm install && npm run build:shared && npm install --workspace=api && npm install --workspace=frontend",
		"deploy:setup": "npm run clean && npm run install:all && npm run build",
		"deploy:start": "npm run start"
	},
	"devDependencies": {
		"typescript": "^5.9.2",
		"@types/node": "^24.3.0",
		"concurrently": "^8.2.2"
	}
}
